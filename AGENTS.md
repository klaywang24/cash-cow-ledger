# AGENTS.md · Cash Cow Ledger（公开仓）

> 本仓公开，不写本机路径、凭据、个人信息。生成于 2026-09-19 05:13 -0400。

## 当前状态（白板 · 最后更新 2026-09-24）

- **待办（交 Codex · Klay 2026-09-24 令 · 不急、不影响数据）**：私有仓混合盘的提交说明日期比数据日晚一天。根因＝私有仓 `.github/workflows/daily.yml` 第 50 行 `git commit -m "data: mixed book level $(date -u +%Y-%m-%d)"` 取的是运行时刻的 UTC 日期，而记账在美东晚上跑、UTC 已过零点（实例：09-25 01:30 ET 那笔写「2026-09-25」，实际新增的是 2026-09-24 行）。改法：说明里的日期取 `mixed_ledger/levels.csv` 本次新增行的 `date`；本次没新增行就不提交。历史提交不改（不重写历史），`ops/test_trigger_audit.py` 已按「读补丁不读说明」归因，不受影响。改完在本条后写「已修 + commit 号」，下次改白板时删掉本条。
- **阶段（发生日 2026-09-23）**：Claude 侧三条现金牛任务全部停用，改由 Codex 的 `07-30-et`、`19-30-et`、`axp` 独立运行；定义保留，切回时必须先停一边再开另一边。
- **下一动作（发生日 2026-09-24 07:30 ET）**：`07-30-et` 核对上一交易日；判红前先读兜底闹钟日志，任务定义与日志位置见本机指针。
- **常态班次（发生时段 07:30 ET／19:30 ET）**：早班查上一交易日，晚班做盘后复核；季度任务由 `axp` 独跑。
- **红线**：当前实开 **19 只，不是 20**；空席只按公开规则在后续调仓处理，不追溯改账。
- **红线**：§9.1 只允许修正指认错误，判断永不可修；旧判断与勘误链必须保留。
- **红线**：同名 LaunchAgent 会静默撞车；新建或切换前先查重，具体调度位置见本机指针。
- **权威**：本节覆盖下方账本，下方旧句只作历史。

## 这个仓是什么
前瞻登记的现金牛纸面指数：规则先公开、冻结、第三方时间戳，再开始记账。记账在云端 GitHub Actions 两趟，第二层复核在私有仓；本机只有兜底闹钟。

## 先读
`HANDOFF.zh.md`、`README-项目地图.md`、`ERRATA.zh.md`（为什么本机不再记账）。

## 硬规矩
- 台账不可变：指认可修，判断永不可修（§9.1）。
- 判红前先读闹钟日志归因，别抢在闹钟出手前叫人。
- 换价源先逐分核对，一列一源。
