# 现金牛台账 · 指数方法论

**版本:v1.0** ｜ 状态:**已定稿,待生效** ｜ **开账日:2026-07-20(周一)**

> 本版本在开账日**之前**公开并被第三方时间戳锚定——这正是「规则未被事后调整」的证据。

> 本文件是第一本(机械指数)的**唯一权威规则来源**。`config.yaml` 是它的可执行副本;
> 二者若有冲突,以本文件为准,并须立即修正 config。
>
> **变更纪律**:本文件的任何修改都必须是**独立的 commit**,标题以 `methodology:` 开头,
> 并写明改了什么、为什么改。**改规则的提交与更新数据的提交彻底分开**,以便任何第三方
> 沿提交历史核验「在某段时间内规则未被改动」。

---

## 0. 这是什么,以及它不承诺什么

一个**向前跟踪的观察指数(paper index)**:按固定规则,从美股中筛选"质量现金流"公司,
每日计算指数点位,每半年调整一次成分。

**它不承诺跑赢任何指数。** 它承诺的只有两件事:

1. **可复现**:任何人拿本文件 + 公开的 SEC EDGAR 数据,能算出与本台账完全相同的结果。
2. **不回填**:开账日之后的每一次计算都被时间戳锚定,**规则不因结果好看与否而事后修改**;
   跑输照登,永不删输家。

**回测不是本产品。** 若日后发布任何回测,必须显著标注「存在前视偏差与幸存者偏差风险」。

---

## 1. 数据源

| 用途 | 源 | 说明 |
|---|---|---|
| 美股财务 | **SEC EDGAR `companyfacts` API** | 免费、无需密钥、XBRL 全历史(约 2009 起) |
| 价格 / 估值 | yfinance | 仅取价格、市值、市盈率 |
| 成分宇宙 | 标普 500 成分快照 | 见 §2 |

**只用 `us-gaap` 税目、只取 `10-K` / `10-K/A` 且 `fp=FY` 的年度值。**
外国私人发行人(只报 20-F、使用 `ifrs-full` 税目者)**结构上不在本指数范围内**——
其财务数据无法用本管线机器复现。

### 1.1 数据缺失

任一必需字段缺失 → 该标的标记 `data_incomplete`,**不参与打分、不入选**,并记录缺失原因。
**禁止用 0、估算值或行业均值填充。**

### 1.2 已知的 XBRL 陷阱与处理(可复现性关键)

1. **同一概念跨会计准则换标签**(如收入在 ASC 606 前后分别用 `Revenues` 与
   `RevenueFromContractWithCustomerExcludingAssessedTax`)→ 每个概念给一组候选标签,
   靠前者优先,靠后者**仅用于补齐前者缺失的年份**(而非"取第一个非空标签就停")。
2. **同一序列内量纲变化**(某些公司某年起改按百万申报)→ 仅当某年偏离序列中位数
   ≈ 精确 1000ⁿ(容差 0.12 个对数级)时才归一;拆股/成长造成的非千倍变化一律放过。
3. **拆股跳变**(股本序列)→ 从新到旧扫相邻年比值,接近常见拆股比例(2/3/1.5/4/5/6/7/8/10/15/20)
   时按比例回溯调整。**股本一律使用加权稀释股本**(`WeightedAverageNumberOfDilutedSharesOutstanding`)。
4. **停报小计导致陈旧值**(如某公司某年起不再申报毛利)→ **陈旧度闸**:任一指标的取值年份
   须 ≥ `该公司收入序列最新财年 − 1`,否则视为缺失。
5. **单步式利润表不报营业利润**(`OperatingIncomeLoss` 缺失)→ 回退
   **EBIT = 税前利润 + 利息费用**。
6. **币种错配**:yfinance 的市值用交易货币、财务数据用报表货币;二者不一致时
   (如 ADR)**不计算 FCF 收益率**,判为不可得。

---

## 2. L1 · 成分宇宙

标普 500 成分快照,**剔除**:

- GICS 行业 `Real Estate` 全部(REIT 以 FFO 而非 FCF 计量,框架不适用)
- GICS 行业 `Financials`,**但保留**子行业 `Transaction & Payment Processing Services`
  与 `Financial Exchanges & Data`

**理由**:自由现金流 / 毛利率 / ROIC 这套框架对**资产负债表型金融**(银行、保险、消费金融)
没有意义——它们的经营现金流包含存贷与浮存变动,ROIC 的"投入资本"口径不成立。
而**资产轻的金融**(支付网络、交易所与数据商)有真实的利润率与自由现金流,故保留。

---

## 3. L2 · 防雷排除(任一命中即剔除)

| 规则 | 阈值 |
|---|---|
| 净利润 / 经营现金流 连续 N 年 > 阈值 | 比值 > 1.3,连续 3 年 |
| 应收账款增速 > 收入增速 × 倍数 | > 2.0 倍,连续 2 年 |
| 净股本近 N 年不降反升(容差 2%) | 回溯 3 年 |
| 净负债 / EBITDA | > 3.0 |

---

## 4. L3 · 质量入选(须全部满足)

| 规则 | 阈值 |
|---|---|
| FCF 连续为正年数 | ≥ 7 年 |
| FCF 变异系数(近 7 年,标准差/均值) | ≤ 0.60 |
| 毛利率 | ≥ 30% |
| 毛利率 10 年趋势 | 末值 ≥ 首值 × 0.95(不下行) |
| ROIC 近 5 年均值 | ≥ 12% |
| 总资产增速 ≤ 收入增速(近 3 年 CAGR,容差 1%) | 资本纪律 |

**营业利润率兜底**:若公司**不申报毛利率相关科目**(如支付网络无 COGS 概念),
则以**营业利润率 ≥ 20%** 替代上述两条毛利率规则。此兜底仅在毛利率不可得时启用。

**FCF = 经营活动现金流 − 购建固定资产支出**。
**ROIC = EBIT × (1 − 有效税率) / (股东权益 + 总债务 − 现金)**;有效税率取
所得税费用/税前利润,落在 [0, 0.6] 之外或不可得时用 21%;投入资本 ≤ 0 时该年不计。

---

## 5. L4 · 估值约束(满足其一即可)

- FCF 收益率(FCF / 市值)**≥ 10 年期美国国债收益率**,**或**
- 市盈率(TTM)**≤ 30**

10 年期美债收益率取自 `^TNX`;不可得时用 4.5% 兜底。

---

## 6. L5 · 成分数量与综合得分

**目标成分数 N = 20。** 合格者不足 15 只时,持有全部合格者并记录异常,
**不补足、不放宽任何规则**。

**综合得分** = 各因子在合格池内 min-max 归一后加权:

| 因子 | 权重 |
|---|---|
| FCF 收益率 | 0.30 |
| ROIC | 0.25 |
| 毛利率 | 0.20 |
| FCF 稳定性(变异系数取负) | 0.15 |
| 低资产增速(取负) | 0.10 |

**双重股权去重**:同一公司多个股票代码只保留得分最高的一只。去重在取前 N **之前**执行:
重复股类不得占席(见 [ERRATA.zh.md](ERRATA.zh.md),2026-07-21)。

---

## 7. 成分调整规则

### 7.1 审查时点

**每年 1 月与 7 月的第一个交易日。其余任何时间,成分一律不变**——
包括期中出现的防雷信号、财报变化或价格剧烈波动。简单即可审计。

### 7.2 名次缓冲带(滞回)

- **新进**:综合得分名次 **≤ 20**,且价格在 **200 日均线上方**(动量否决)。
- **剔除**:在位成分名次跌至 **> 40**,或当期不再通过 L2/L3/L4。
  **例外**:该剔除但价格仍在 200 日均线上方者,**延后一个审查期**。
- 剔除产生的空位,由未持有者中名次最高的依次补足至 N = 20。

用**绝对名次**而非百分位:合格池仅二十余只时,"前 20%"会退化成只剩四五只。

### 7.3 权重

- **仅在入场时定权重**:新进成分按综合得分比例分配,**单只封顶 8%**,
  超出部分按比例分给未封顶者(迭代至收敛)。
- **入场后永不再平衡。** 权重随价格自由漂移,赢家膨胀不予削减。
- 剔除者释放的权重优先分配给新进者;有余则按比例分给在位成分
  (按比例分配**保持在位成分之间的相对权重不变**,故不违反"不削赢家")。

**为何按得分而非 FCF 绝对额加权**:按 FCF 绝对额会系统性偏向体量大的成熟公司
(实测:Altria 权重由 11.8% 降至 6.8%,最大/最小极差由 16× 收敛至 2.3×)。
卫星组合应按**吸引力**而非**体量**下注。

### 7.4 换手

年换手超过 25% 时**记录告警**。告警仅作记录,**不触发任何规则调整**。

---

## 8. 指数点位

- 基点 **100**,起始于开账日。
- 以**复权收盘价**计算(近似含股息的总回报口径)。
- **每个交易日计算一次**并落库。成分不变期间,点位仅随价格变化。

---

## 9. 不可篡改性

- 全部数据与本方法论托管于**公开 Git 仓库**,提交时间由 GitHub 第三方背书。
- 每次数据更新后向 **Internet Archive (Wayback Machine)** 提交快照。
- 提交类型严格分离:`methodology:`(规则变更)/ `data:`(数据更新)/ 其他。
- **官方源日后修订历史数据时,本台账旧记录保持原样**,修订另行注明,绝不悄悄覆盖。

### 9.1 差错修正政策(v1.1,2026-08-06 首次执行)

上面那条「只增不改」,目的是让外人无需信任作者也能验证记录。它是手段不是目的:一条明知
是错的记录留在原地,不是在保护可验证性,而是在摧毁它。两者冲突时,以本节为准。指数公司
与基金行政管理人处理净值差错走的正是这条路:既不悄悄改,也不明知有错还照登,而是按公开
程序重述(restatement)。

**什么可以改:仅限指认性错误。** 标签(日期、代码、归属位置)指错了对象,而记录的值本身
可被独立验证确属修正后的标签。外人可用的判据:修正之后,这条记录用一手数据源验证起来必须
变得**更容易**而不是更难。

**什么永远不能改:判断。** 点位、价格、权重、得分、成分取舍。数值错了,行原样留着,修的
是机制,错误记入 ERRATA。先例:2026-07-20 开账 19 只永久保持原样(见 ERRATA 2026-07-21 条)。

**程序,四步,顺序不可乱:**

1. **独立复算。** 用第二套实现从一手数据源复现修正后的事实,不许用制造这个错误的那套代码。
2. **勘误先行。** 根因、证据、影响面,在推送修正之前写进 ERRATA,与修正同一提交序列。
3. **只用普通提交。** 绝不改写历史、绝不 force push;出错的提交与修正的提交永久同时可见,
   任何人用 `git log -p data/` 即可审计全过程。
4. **机制同行。** 制造该错误的缺陷在同一序列内修复,并加上复发即大声报错的闸。

修正在 ERRATA 中编号。不走此程序的任何修改,一律视同篡改,作者本人也不例外。

---

## 10. 明确不做的事

- 不自动交易、不接任何券商 API。
- 不因回测好看而放宽规则。
- 不在数据缺失时用估算值继续计算。
- 不构成任何投资建议。

---

## 11. 判据规范(严进宽出)

本节是**元规则**:它不规定选什么股,它规定本仓所有判据必须怎么写。新增任何闸、监控、
校验或数据读取,一律受本节约束。

### 11.1 三态:每个判据必须能说「我不知道」

**任何判据都必须能区分三种状态,而不是两种:是 / 否 / 我不知道。**

把「我不知道」压成一个确定答案,是本仓迄今每一次故障的同一个母体:

| 现场 | 三态 | 被压成 | 后果 |
|---|---|---|---|
| ERRATA 2026-08-06 | 交易日是 D / 不是 D / 时钟不知道 | 「是 D」 | 一行发在错误日期下 |
| ERRATA 2026-08-18 | 有交易有数据 / 没开市 / **有交易但源没数据** | 「没开市」 | 监控诬告台账造假 |
| §7.1 审查闸 | 审查发生了 / 没发生 / **发生了但没留痕** | 「没发生」 | 整个 1 月每天重跑 |
| 附二状态表 | 实现了 / 没实现 / **写了但没验证** | 「实现了」 | ✅ 沦为装饰 |

其窄版早已写在 ERRATA 2026-08-06:**「时钟只有否决权,永远没有供给权。」**
宽版是:**任何不可能知道答案的来源,必须被允许说「我不知道」;这句「不知道」永远不得
被静默渲染成一个答案。**

推论:**日期算术(星期几)是日期自身的属性,不是读时钟**,故可用于否决,不违反本条。

### 11.2 严进:检测无条件,且必须带归属

- **入口一律严。** 任何歧义都必须被检测、被记录,绝不静默通过。
- **报错必须说清是谁坏了。** 第三态的完整形态不是「我不知道」,而是
  **「我不知道,而且我知道是谁让我不知道的」**。

归属错误的告警和检测失败一样有害:它耗掉一小时,并教会读者忽略下一条。
2026-08-18 的监控**检测对了、归属错了**——它指控台账伪造,真相是数据商有洞。
故出口码必须承载归属:**1 = 台账有问题,2 = 源看不见**,不得混用。

### 11.3 宽出:响应按可逆性分档

「先报错、再决定」不能一刀切,否则会撞上告警通胀与无人值守两个真问题。分档判据只有
一句话:**这一步可不可逆。**

| 档 | 判据 | 出口行为 | 实例 |
|---|---|---|---|
| **停** | 写入不可篡改的记录 | 拒绝执行,大声报错,等人 | 调仓补席、写 `index_level.csv` |
| **让** | 只影响衍生物 | 跳过该步,管线继续,记账 | 图表、README |
| **喊** | 完全可逆 | 照常报,不阻断 | 监控告警本身 |

**边界:「宽出」只约束向前的动作,不豁免已发布的错误。** 已认定的指认性错误按 §9.1
立即修正,不适用「先等等」。

### 11.4 验收:没报过红的闸不算存在

- 附二每新增一行,必须附带一个**已被证明会报红的测试**;做不到就记 ⛔,不许记 ✅。
- 判据的测试必须**打到判据本身**,而不是打到一个复述判据的副本。
- 元判据(沿用既有纪律):**「如果它是好的,我的脚本会不会照样这么报?」**
  故本仓的闸须经**变异测试**:把闸打回已知的错误行为,测试必须变红。
- **测试断言的是「这个函数算的是什么」,不是「这个答案该被拿去干什么」。**
  2026-08-18 的 selftest 全绿而生产报红,正因为它验的是
  `last_completed_from`(最后一个**有数据**的交易日,答案正确),
  而缺陷在 `main()` 拿这个答案去回答了另一个问题(最后一个**交易日**)。
  **函数名断言了函数并不知道的事。** 故凡有判据,既要测它的取值,也要测**据其作出的裁决**。

---

## 附:人工裁量不属于本方法论

本指数**不含任何人工裁量成分**。作者个人的裁量持仓(如有)记录于本仓库之外,
**与本指数完全隔离,不影响本指数的任何计算**。分离的目的,是让本指数保持
100% 机械、可复现、可审计——任何裁量都会破坏这三点。

---

## 变更历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-07-18 | 首版。开账前定稿并公开锚定；开账日定为 2026-07-20。 |
| v1.0.1 | 2026-07-21 | 仅机制修复,规则与阈值不变:双重股权去重移到取前 N 之前;候选文件输出完整排名;新增重复占席守卫([ERRATA.zh.md](ERRATA.zh.md))。 |
| v1.1 | 2026-08-06 | 新增 §9.1 差错修正政策(指认性错误可按程序修正,判断永不可改);日期标签一律取自行情数据自带的美东交易日,时钟只有否决权;新增拒写闸与按交易日判stale的监控。筛选规则与阈值全部不变。起因见 [ERRATA.zh.md](ERRATA.zh.md) 2026-08-06 条。 |

---

## 附二:实现状态对照(防「文档写了、代码没有」)

本表用于核验方法论的每一条是否真的落进了代码。**新增规则时必须同步更新本表**——
「文档承诺了但代码没实现」是最容易在半年后变成违规的一类缺陷。

| 方法论条款 | 实现位置 | 状态 |
|---|---|---|
| §1 数据源与 XBRL 六坑 | `src/edgar.py` / `src/metrics.py` | ✅ |
| §2 L1 宇宙与金融/REIT 剔除 | `src/run_screen.py::load_universe` | ✅ |
| §3 L2 防雷 | `src/screen.py::screen_us` | ✅ |
| §4 L3 质量 + 营业利润率兜底 | `src/screen.py::screen_us` | ✅ |
| §5 L4 估值 | `src/screen.py::apply_valuation` | ✅ |
| §6 L5 数量与综合得分 | `src/run_screen.py::score_pool` / `build_portfolio` | ✅ |
| §6 双重股权去重(取前 N 之前) | `src/screen.py::dedup_dual_class` + `build_portfolio` 守卫 | ✅ |
| §7.1 审查时点(**1月/7月的第一个交易日**) | `src/reconstitute.py::review_due` + 登记簿 `data/ledger/review_log.csv` | ✅(2026-08-18 修正,见下) |
| §7.2 名次缓冲带 + 双向动量否决 | `src/reconstitute.py` | ✅ |
| §7.3 权重与「入场后不再平衡」 | `src/build_portfolio.py` / `open_books.py` / `reconstitute.py` | ✅ |
| §7.3 **空席无剔除可供资金时如何入册** | — | ⛔ **条款不存在**,见下方未决项 |
| §7.4 换手预算告警 | `src/reconstitute.py` | ✅ |
| §8 指数点位(份数机制) | `src/daily_level.py` | ✅ |
| §9 不可篡改(公开库/Wayback/提交分类) | `.github/workflows/daily.yml` | ✅ |
| — 台账新鲜度监控 | `src/check_freshness.py` + `monitor.yml` | ✅ |
| — **不变量回归测试有人跑** | `tests/` + `daily.yml::regression invariants` | ✅(2026-08-18 接上) |
| §11.1 三态日历(交易且有数据/没开市/源有洞) | `check_freshness::calendar_from` | ✅(4 条样本 + 变异) |
| §11.2 出口码承载归属(1=台账 2=源) | `check_freshness::verdict_from` | ✅(7 条裁决样本 + 变异) |
| §11.3 补席按「停」档处理 | `reconstitute.py` 空席告警 | ✅ |
| §11.4 附二每行必须有能报红的测试 | 本表 + `--selftest` / `tests/` | ✅ |
| — **台账中间缺口检测**(旧闸只看最后一行) | `check_freshness::verdict_from` GAP | ✅(2026-08-18 新增) |

**本表自己也会骗人。**上面 §7.1 这一格在 2026-08-18 之前一直是 ✅,而代码实现的是
「1 月/7 月的**任意一天**」,不是文档写的「第一个交易日」;且「本月已调过」靠有没有成分
携带当月 `entry_date` 反推,于是**零变更或只剔除不新进的审查不留痕迹,闸不跳,整个 1 月
每天重跑**。打勾时无人以负向样本验证过,勾就成了装饰。此后新增行必须附带一个**能报红的
测试**,否则一律记 ⛔ 而非 ✅。

同日另发现:`tests/test_dedup.py` 自 `fb940ea` 起被 ERRATA 列为去重缺陷的回归闸,
但**全仓没有任何调度器跑过它**。闸不被跑等于不存在——已接入 `daily.yml`,且在管线
碰台账**之前**跑。

### 未决项(需人工裁定,不得由代码自行其是)

**空席的资金来源。**ERRATA 2026-07-21 承诺:开账时被 FOXA 烧掉的第 20 席,
「保留到 2027 年 1 月的调仓日,由 §7 的标准入册规则把成分补回 N = 20」。但 §7.3 只
规定了**由剔除释放的市值**为新进者供资;当空席存在而当期无任何剔除时(`freed == 0`),
条款是空白的,代码因此不会补席。2026-08-18 用实时价格演练 2027-01 调仓,该情形确实
发生:19 只全部留任,DLTR 名次与动量双闸皆过,**因无资金而无法入册**。

在裁定落定前,代码**大声告警且不擅自补席**——纸上指数没有现金,任何补席方案都必然
稀释在册成分,而「入场后永不再平衡、赢家不予削减」是 §7.3 的明文承诺。**这是承诺与
机制的冲突,按既定原则应改机制、不改承诺**,但改法必须由人选定并版本化记录。
