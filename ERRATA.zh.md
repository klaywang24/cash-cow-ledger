# 勘误

缺陷在此带日期公开记录。**数值与判断**永不回改:错了,改的是机制,错误的记录原样留存。
**指认性错误**(标签指错了对象)仅可按 METHODOLOGY §9.1 的编号程序修正:独立复算、
勘误先行、只用普通提交。勘误史是资产,不是负债。

**最后核验:2026-08-06。**下方共两条。2026-07-21 至 2026-08-06 每一日的公开点位均已
用交易所价源独立复算,与公布数字相差不超过 0.0001;`constituents.csv` 与开账提交
逐字节一致。**这一行记录的是「本日志是当前的」,而不只是「没人更新过」。**

---

## 2026-08-06 · 一行数据发布在错误的日期下,§9.1 第 1 号修正

**发生了什么。**2026-08-06 的收盘被发布成 `2026-08-07,104.1012,19`:标签是一个还没
发生的交易日,而 08-06 自己看起来缺了一格。值是对的,错的只有标签。

**原因,三层。**

1. **导火索。**GitHub Actions 当日 15:22 至次日 00:05 UTC 发生长达九小时的事故
   (官方状态页原文:workflow runs are failing to start or delayed),当天的定时
   触发被吞掉。人工补跑发生在 00:42 UTC:UTC 已过午夜,纽约还是 08-06。
2. **缺陷。**`daily_level.py` 用 `date.today()`(runner 的 UTC 时钟)给行打日期,
   而不是用 K 线数据自己所属的交易日。此前每天都对,只因排班(21:30 UTC = 纽约
   下午)恰好让两套日历同天。**对是时刻表的性质,不是代码的性质。**
3. **流程。**同一缺陷数周前已在姊妹仓库发现并修复,注释里连触发条件(CI 跨过
   UTC 午夜)都写明了,但修复从未同步到本仓。**修 bug 必须按类扫,不能按处修。**

**修正的证据。**用第二套独立实现(纯标准库,直取交易所数据端点,与管线零共享代码)
复算:08-03 / 08-04 / 08-05 三日与已发布点位四位小数全部吻合;**2026-08-06** 算得
104.1012;19 只成分股最后一根 K 线全部落在 08-06;发布时点不存在 08-07 的交易时段。

**修正本身。**按同一提交序列内确立的 METHODOLOGY §9.1,该行日期字段由
`2026-08-07 → 2026-08-06`,值未动。普通提交,历史未改写:出错的提交与本修正
永久同时可见(`git log -p data/ledger/index_level.csv`)。若留错行不改,真正的
08-07 收盘将被判「已记录」而跳过:明知是假的记录不保全完整性,只让错误复利。

**为让这一类死绝,改了什么。**

- 日期标签一律取自 K 线自带时间戳折算的美东交易日。时钟只有否决权(未来日期拒写),
  永远没有供给权。
- 两道拒写闸:成分股对最后交易日不一致、K 线日期在未来,均大声中止。负向样本实测报红。
- 仓内其余 5 处 `date.today()` 全部钉死 America/New_York。
- 监控改为用 SPY 自己的 K 线判定「最近一个已完结交易日」,美东零点后即跑,缺一天
  当晚就报,出现未来日期的行同样报红。旧的 4 天日历阈值(会让本次事故静默数日)废除。
- 主定时触发两小时后加一班备用触发(管线幂等),单次 cron 事件被吞不再损失一天。

**没改什么。**每一个已发布点位。`constituents.csv`。git 历史。

---

## 2026-07-21 · 开账日入册 19 只,目标是 20 只

**发生了什么。**开账日(2026-07-20)台账按 19 只成分入册,目标 N = 20
(`data/ledger/index_level.csv`:`n_constituents=19`)。

**原因。**确定性的代码缺陷。与数据、网络、数据商都无关:当天 EDGAR、价格源、CI
全部正常,每只入账价与交易所收盘价逐分吻合。缺陷在于:筛选程序先把候选名单截断到前
20 名,**之后**才做双重股权去重。FOX 与 FOXA(福克斯公司的两个股类,分列第 10、11 名)
各占一席,去重删掉 FOXA 后没有递补。当天共 23 家通过 L4,缺席本可避免:按综合得分,
第 20 席属于 GRMN(佳明,0.2503)。持仓下限(15)远低于 19,守卫没有触发。

**同一条缝上的第二个缺陷,已提前修掉。**被截断的候选文件同时废掉了 §7.2 的退出
缓冲带:在册成分若排在 21 至 40 名,调仓时会因文件里不存在 20 名以后的名次而被误判为
「不再通过 L2/L3/L4」遭错误剔除。

**改了什么(commit `fb940ea`)。**

- 去重移到一切截断之前,收敛为一个共享函数(`src/screen.py::dedup_dual_class`):
  重复股类不再可能占席。
- `candidates_*.csv` 现在输出 L4 幸存者去重后的**完整**排名,不再只有前 20,
  §7.2 退出缓冲带因此有名次可用。
- `build_portfolio` 对仍含重复股类的候选文件硬拒绝:同类问题再发生时管线大声失败,
  而不是静默缩水。
- 用开账日漏斗数据钉死回归测试(`tests/test_dedup.py`):20 家不同公司,GRMN 在第 20 席。

**没改什么。**台账本身。2026-07-20 的记录原样成立:19 只成分,权重合计为 1,基准 100。
GRMN **不**追溯补入:回改开账记录会让前向台账失去存在的意义。已发布的点位对入册的
这本账而言是正确的。空席保留到 2027 年 1 月的调仓日,由 §7 的标准入册规则
(名次 ≤ 20 加动量闸)把成分补回 N = 20。
